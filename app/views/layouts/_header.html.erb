<nav class='navbar navbar-inverse navbar-fixed-top'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <button type='button' class='navbar-toggle collapsed'
              data-toggle='collapse' data-target='#navbar'
              aria-expanded='false' aria-controls='navbar'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
      <a class='pull-left' href='<%= link_to '/' %>'>
        <%= image_tag('livogis64.png', class: %w(livogis-img)) %>
      </a>
      <%= link_to 'Livogis', '/', class: %w(navbar-brand livogis-title) %>
      <span class='navbar-brand livogis-sub-title'>Chauffeurs</span>
    </div>
    <div style='height: 1px;' aria-expanded='false' id='navbar'
         class='navbar-collapse collapse'>
      <div class='nav navbar-right'>
        <% if user_signed_in? %>
          <%= link_to 'Déconnexion',
          destroy_user_session_path,
          method: :delete %>
          <span style="font-size:24px; color: white">|</span>
          <a href='/user/profil'><%= current_user.lastname.upcase %>
                                 <%= current_user.firstname %>
          </a>
          <%= image_tag 'avatar64.png',
          class: %w(livogis-img) %>
        <% else %>
          <%= link_to 'Connexion', new_user_session_path  %>
          <span style="font-size:24px; color: white">|</span>
          <%= link_to 'Inscription', new_user_registration_path  %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
